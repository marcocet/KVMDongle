.venv/
__pycache__/
*.pyc
*.cc-history
.DS_Store

# PyInstaller build output -- large, platform-specific, regenerated by
# packaging/build_*.{ps1,sh}, never committed. packaging/client.spec
# itself (the build definition) is source and stays tracked.
/build/
/dist/