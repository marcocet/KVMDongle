# dnsmasq.conf -- DHCP for the ISO-upload Wi-Fi AP (see wifi-ap-toggle.sh).
# Installed as /etc/dnsmasq.d/kvmdongle.conf, active only while the AP is on.
interface=wlan0
bind-interfaces
dhcp-range=192.168.50.10,192.168.50.100,255.255.255.0,24h
